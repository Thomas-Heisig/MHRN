# ============================================================================
# MHRN — Hugging Face Space Configuration
# ============================================================================
# Diese Konfiguration definiert einen optionalen Hugging Face Space,
# der das MHRN Operator Dashboard als Web-App bereitstellt.
#
# Space: https://huggingface.co/spaces/ThomasHeisig/MHRN-Space
# Dieses Space wird als separates Repository vom kanonischen GitHub-Branch gespeist.
# ============================================================================

# SDK-Typ (docker, streamlit, gradio, static)
sdk: docker

# Dockerfile-Pfad (relativ zum Space-Root)
dockerfile: Dockerfile

# Container Port (muss mit EXPOSE im Dockerfile übereinstimmen)
port: 8765

# Startup Command
command: python -m src.main --config configs/poc_config.yaml

# Umgebungsvariablen
env:
  - name: BRAIN5D_HEADLESS
    value: "false"
  - name: BRAIN5D_DASHBOARD_PORT
    value: "8765"
  - name: BRAIN5D_DASHBOARD_HOST
    value: "0.0.0.0"

# Hardware-Anforderungen
hardware:
  cpu: "basic"       # basic, medium, large
  memory: "8GB"
  gpu: false

# Ländereinstellung
country: "us"
